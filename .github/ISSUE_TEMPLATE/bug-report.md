---
name: Bug di codice
about: Segnala un comportamento del sito o degli script diverso da quanto documentato o testato
title: "[BUG] "
labels: bug
---

## Descrizione del problema

Descrivi cosa succede e cosa ti aspettavi che succedesse invece.

## Passi per riprodurre

1.
2.
3.

## Ambiente

- Comando eseguito (`npm run build`, `npm test`, `npm run check`, sito in produzione, ecc.):
- Versione di Node (`node --version`):
- Sistema operativo:
- URL della pagina, se applicabile:

## Output osservato

```
(incolla qui l'output completo del comando o dell'errore)
```

## Note aggiuntive

Qualunque altro contesto utile (screenshot, link, log).

---

Ricorda: questo template è per problemi di **codice** (`src/`, `scripts/`,
`netlify/functions/`, `tests/`). Per correzioni al contenuto delle quattro
Knowledge Base canoniche in `content/kb/`, usa invece il template
"Segnalazione contenuto KB" — vedi `CONTRIBUTING.md` per la politica
completa di modifica delle KB canoniche.
