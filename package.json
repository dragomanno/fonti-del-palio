{
  "name": "fonti-del-palio",
  "version": "0.1.0",
  "private": true,
  "description": "Progetto documentale indipendente, non ufficiale e ad accesso pubblico per la consultazione del corpus documentale del Protocollo Equino del Palio di Siena.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "check": "node scripts/check-env.mjs && tsc --noEmit",
    "build": "node scripts/check-env.mjs && node scripts/build-content-index.mjs && astro build && node scripts/build-sitemap.mjs && pagefind --site dist",
    "preview": "astro preview",
    "chunk:diagnostic": "node scripts/chunk-diagnostic.mjs",
    "index:build": "node scripts/build-content-index.mjs",
    "embeddings:build": "echo 'embeddings:build e riservato alla fase A2 (post-M6). Vedi data/static-site-milestone-plan.md.' && exit 1",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "search:index": "pagefind --site dist",
    "sitemap:build": "node scripts/build-sitemap.mjs"
  },
  "dependencies": {
    "@fontsource/fraunces": "^5.3.0",
    "@fontsource/inter": "^5.3.0",
    "@fontsource/jetbrains-mono": "^5.3.0",
    "@netlify/functions": "^3.0.0",
    "astro": "^5.1.0",
    "marked": "^14.1.4"
  },
  "devDependencies": {
    "pagefind": "^1.5.2",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
